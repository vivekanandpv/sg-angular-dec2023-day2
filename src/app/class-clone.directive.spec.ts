import { ClassCloneDirective } from './class-clone.directive';

describe('ClassCloneDirective', () => {
  it('should create an instance', () => {
    const directive = new ClassCloneDirective();
    expect(directive).toBeTruthy();
  });
});
